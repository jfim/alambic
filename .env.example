# Generate with: mix phx.gen.secret
SECRET_KEY_BASE=

# Public hostname (no scheme), e.g. alambic.example.com
PHX_HOST=

# Base URL of the archive API (used by the correction UI to fetch raw HTML)
ARCHIVE_BASE_URL=

# Postgres
POSTGRES_PASSWORD=alambic

# Review queue threshold — items with confidence below this are queued
REVIEW_CONFIDENCE_THRESHOLD=0.7

# Optional, defaults to 4000
PORT=4000

# Cham filename for the raw HTML artifact per item
CHAM_RAW_HTML_FILENAME=original.html
